@model IEnumerable<RolesIS.Models.Compra>
@{
    ViewBag.Title = "Index";
}

<br />
<h4>Mis Compras</h4>

<table class="table">

    <tr>
        <th>Producto</th>
        <th>Proveedor</th>
        <th>Cantidad</th>
        <th>Importe</th>
        <th>Cuenta</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Producto.Nombre</td>
            <td>@item.Producto.Anunciante.UserName</td>
            <td>@item.Cantidad</td>
            <td>@item.Importe</td>
            <td>@item.Cuenta</td>
        </tr>
    }

</table>

